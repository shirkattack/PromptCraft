# Runtime dependencies. pyproject.toml is the source of truth -- keep the two in
# sync, or install with `pip install -e ".[dev,test]"` instead of this file.
#
# fastapi>=0.116.1 is a hard floor, not a preference: it pulls starlette>=0.40,
# and starlette<0.37 is incompatible with httpx>=0.28 (TestClient raises
# "Client.__init__() got an unexpected keyword argument 'app'").
alembic>=1.16.4
dspy-ai>=3.0.2
fastapi>=0.116.1
httpx>=0.28.1
pydantic>=2.11.7
pydantic-settings>=2.10.1
python-dotenv>=1.1.1
python-json-logger>=2.0.7
python-multipart>=0.0.20
sqlalchemy>=2.0.43
uvicorn[standard]>=0.35.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
