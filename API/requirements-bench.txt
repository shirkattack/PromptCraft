# Only for scripts/build_mbppplus_dataset.py (downloads MBPP+ from EvalPlus)
# and for cross-checking results with `evalplus.evaluate`. Not an API runtime
# dependency: the "tests" metric itself is standard library only, and this file
# is deliberately not a pyproject extra so `uv sync --all-extras` leaves it out.
#
# Use it as an overlay on the API environment, which keeps API/.venv untouched:
#
#   uv run --project API --with-requirements API/requirements-bench.txt \
#       python scripts/build_mbppplus_dataset.py
#
evalplus>=0.3.1
# Fallback source when the evalplus import fails: datasets.load_dataset("evalplus/mbppplus")
datasets>=2.19
